Conclave
├── backend/                     # Rust (Axum) 后端：负责 Prompt 组装、EJS、正则、状态管理
│   ├── Cargo.toml
│   ├── src/
│   │   ├── main.rs              # Axum 服务器入口与路由
│   │   ├── card_loader.rs       # 解析 V3 JSON 卡片
│   │   ├── lorebook/
│   │   │   ├── mod.rs           # 世界书管理
│   │   │   └── ejs_parser.rs    # 核心：Rust 实现的轻量级 EJS 解释器
│   │   ├── pipeline/
│   │   │   ├── regex_engine.rs  # 核心：执行卡片的 regex_scripts
│   │   │   └── mvu_patch.rs     # 核心：处理 AI 返回的 JSONPatch
│   │   └── llm_proxy.rs         # 代理请求大模型 API
│   └── data/
│       └── cangxuan_v1.0.20.json
└── frontend/                    # React (Vite) 前端：负责 UI 与 Iframe 沙盒
    ├── package.json
    ├── src/
    │   ├── App.tsx
    │   ├── components/
    │   │   └── IframeSandbox.tsx # 核心：隔离执行卡片的 HTML/JS/jQuery
    │   └── store/
    │       └── gameStore.ts     # Zustand 状态同步
    └── index.html