/**
 * st-host extensions public surface (P2 skeleton).
 * @module st-host/extensions
 */

export {
  createExtensionManager,
  normalizeManifest,
  isRemoteExtensionEntryAllowed,
} from './ExtensionManager.js';
